{
  "name": "animatch-backend",
  "version": "1.0.0",
  "description": "Backend API for AniMatch — handles AI-powered filler episode lookups via Groq, keeping the API key secure.",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}